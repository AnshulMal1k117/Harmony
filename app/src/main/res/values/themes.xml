<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Harmony" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/intense_burgundy</item>
    </style>

    <style name="splash_screen" parent="Theme.Material3.DayNight">
    <item name="android:windowBackground">@drawable/harmony_splash_screen</item>
    </style>
</resources>